"""
Celery를 사용한 비동기 백그라운드 작업을 처리하는 워커(Worker) 패키지입니다.

웹 요청-응답 사이클과 분리하여 시간이 오래 걸리는 작업을 처리하기 위해 사용됩니다.
예를 들어, 사용자가 대용량 문서를 업로드하면, API 서버는 즉시 사용자에게
'업로드 접수' 응답을 보낸 후, 실제 문서 처리(분할, 임베딩, 저장) 작업은
Celery 워커에게 비동기적으로 위임합니다.

이 패키지는 다음을 포함합니다:
- `celery_app.py`: Celery 애플리케이션 인스턴스를 생성하고 설정합니다.
- `tasks.py`: 실제 비동기 작업 로직(예: `process_document`)을 `@celery_app.task`
  데코레이터를 사용하여 정의합니다.
"""
